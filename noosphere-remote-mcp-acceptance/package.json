{
  "name": "@noosphere/remote-mcp-acceptance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-client continuity acceptance suite for Noosphere Remote Project Memory (PR5). Tests only; drives the merged PR4 server via the MCP SDK client.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@noosphere/remote-mcp-contracts": "file:../noosphere-remote-mcp",
    "jose": "^6.0.0"
  }
}
