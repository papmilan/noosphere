{
  "name": "@noosphere/remote-mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Streamable HTTP MCP resource server for Noosphere Remote Project Memory (PR4).",
  "scripts": {
    "check": "node --check src/server.js && node --check src/mcp-core.js && node --check src/config.js && node --check src/logging.js",
    "test": "npm run check && node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@noosphere/remote-mcp-contracts": "file:../noosphere-remote-mcp"
  },
  "devDependencies": {
    "jose": "^6.0.0"
  }
}
