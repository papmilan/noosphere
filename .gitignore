# Dependencies
node_modules/

# Runtime state
demo_blobs/
project_registry.json
*.tmp

# Test temp dirs
tests/.tmp-*

# Environment secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store
._*

# Local AI tool state (never part of the project)
.claude/
.claude-flow/
.serena/
*-worktree

# Local screenshots and design references
/*.png

# Noosphere local runtime files (protocol.json stays tracked)
.noosphere/context.md
.noosphere/state.json
.noosphere/ide-hint.json
.noosphere/*.tmp
.noosphere/journal.md
noosphere-relayer/.noosphere-local-memory.json
noosphere-relayer/.noosphere-runtime/
